# سياسة الأمن — ذكاء (Zakae)

## نطاق المشروع
موقع ثابت (HTML/CSS/JS) على GitHub Pages، دون خادم خلفي ولا قاعدة بيانات ولا
مصادقة مستخدمين. سطح الهجوم محدود عمداً.

## الضوابط المطبّقة
- **لا مصادر XSS:** يُمنع `innerHTML` / `outerHTML` / `document.write` / `eval`؛
  كل الواجهات المعتمدة على البيانات تُبنى بـ `textContent` + DOM. تفرضه بوابة الجودة آلياً.
- **لا أسرار في المستودع:** لا مفاتيح API ولا رموز سرّية. المعرّفات العامة
  (GTM، GA4، Formspree) عامة بطبيعتها ولا تمنح صلاحيات كتابة.
- **مدخلات المستخدم:** نموذج التواصل يُرسَل عبر خدمة خارجية موثوقة مع honeypot
  لمكافحة الرسائل المزعجة؛ لا تنفيذ من طرف العميل لمحتوى المستخدم.
- **التبعيات الخارجية:** مقصورة على الخطوط والتحليلات؛ أي إضافة جديدة تتطلب مراجعة.

## الإبلاغ عن ثغرة
إذا اكتشفت ثغرة أمنية، راسلنا على **hello@zakae.com** مع وصف وخطوات إعادة الإنتاج.
لا تفتح Issue عاماً بتفاصيل قابلة للاستغلال. نلتزم بالرد خلال مدة معقولة والإصلاح
حسب الخطورة.

## اتجاهات التحصين المستقبلية
- إضافة رؤوس أمان (CSP، `X-Content-Type-Options`، `Referrer-Policy`) بعد تقليل
  السكربتات المضمّنة — عبر وسوم `<meta>` المدعومة أو طبقة استضافة تدعم الرؤوس.
- توحيد التحليلات في GTM لتقليل السكربتات المضمّنة.
