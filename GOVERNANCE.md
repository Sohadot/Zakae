# حوكمة ذكاء (Zakae) — معايير الأصل الرقمي

هذه الوثيقة هي **العقد الملزم** لكل ما يُضاف إلى [zakae.com](https://zakae.com).
هدفها بناء **مصداقية وموثوقية** تتراكم مع الوقت، وتعظيم قيمة الأصل عبر
**محتوى عميق، انضباط تقني وأمني، و SEO قوي** — لا سرعة على حساب الجودة.

القاعدة الأولى: **المصداقية قبل النمو، والجودة قبل الكمية.**

---

## 1. الإطار المفاهيمي للمحتوى

المحتوى مُنظَّم في أربع طبقات مترابطة، من المفهوم إلى التطبيق:

| الطبقة | الغرض | الموقع |
|--------|-------|--------|
| **المصطلحات** (Concepts) | تأسيس المفاهيم الجوهرية بلغة عربية دقيقة | `data.json → glossary` |
| **النماذج** (Models) | مرجع تحليلي لكل نموذج: قدراته وحدوده واستخداماته | `models/*.html` + `data.json → models` |
| **المقارنات** (Comparisons) | مواجهة النماذج على محاور موضوعية قابلة للقياس | جدول الرئيسية + مقالات `*-vs-*` |
| **الأدلة** (Guides) | تطبيق عملي: كيف تستخدم، متى تختار | `articles/*.html` |

**سلّم الصعوبة** موحّد عبر الطبقات: `beginner → intermediate → advanced`.
كل مصطلح/صفحة يحمل تصنيفاً (`category`) ومستوى (`difficulty`) لبناء مسارات تعلّم متماسكة لاحقاً.

**مبدأ التماسك:** كل مقالة تربط بالمصطلحات وصفحات النماذج ذات الصلة داخلياً — لا محتوى معزول.

---

## 2. معايير المحتوى التحريرية (صارمة)

- **العمق قبل العدد.** مقالة واحدة عميقة موثّقة أفضل من خمس سطحية.
- **الدقة أولاً.** لا ادّعاء دون أساس؛ عند الشك، لا يُنشر. تُحدَّث الأرقام والحقائق عند تغيّرها.
- **أصالة عربية.** كتابة عربية أصيلة محرَّرة — **لا ترجمة آلية حرفية ولا حشو**. المصطلح الأجنبي يُذكر ثم يُعرَّب.
- **حياد تحليلي.** لا انحياز تسويقي لنموذج؛ تُذكر نقاط القوة والضعف بإنصاف.
- **صدق الأرقام.** أي رقم معروض (إحصائيات، «+30 مصطلح») **يجب أن يطابق المحتوى الفعلي**. تفرض البوابة هذا آلياً.

---

## 3. المعايير التقنية

- **بلا اعتماديات:** HTML/CSS/JS خام. لا bundler ولا npm. الخطوط والتحليلات فقط خارجية.
- **RTL دائماً:** `<html lang="ar" dir="rtl">`.
- **DOM آمن:** بناء الواجهات المعتمدة على البيانات عبر `createElement` + `textContent` — **يُمنع** `innerHTML` لحقول النص.
- **CSS مشترك** تحت `/assets/css/`؛ تجنّب التكرار.
- **الروابط جذرية** (`/models/...`) لتعمل من أي عمق.
- **الأداء:** صور مضغوطة بأبعاد محدّدة، `preconnect` للخطوط، لا سكربتات ثقيلة.

---

## 4. المعايير الأمنية

- **يُمنع** `innerHTML` / `outerHTML` / `document.write` / `eval` — دفاع في العمق ضد XSS. تفرضه البوابة.
- **لا أسرار في المستودع:** لا مفاتيح API ولا رموز. معرّفات النماذج (Formspree, GTM) عامة بطبيعتها فقط.
- **مدخلات المستخدم** (نموذج التواصل) تُعالَج عبر خدمة موثوقة (Formspree) مع honeypot — لا تنفيذ من طرف العميل.
- **سياسة التبعية:** أي مصدر خارجي جديد (سكربت/خط/CDN) يتطلب مبرّراً في وصف الـ PR.
- الإبلاغ عن الثغرات: راجع [`SECURITY.md`](./SECURITY.md).

---

## 5. معايير SEO

- **عنوان ووصف فريدان** لكل صفحة.
- **`canonical` مطابق** للرابط الحقيقي (index داخل مجلد ⇐ مسار المجلد بشرطة مائلة).
- **JSON-LD** مناسب لنوع الصفحة على **كل** صفحة (WebSite / Article / AboutPage / ContactPage…).
- **`sitemap.xml`** يبقى متزامناً مع الصفحات الحيّة؛ يُحدَّث `lastmod` عند تغيّر المحتوى.
- **سياسة اللغات:** لا مسارات `/ar/` أو `/en/` ولا `hreflang` قبل وجود صفحات فعلية.
- روابط داخلية سليمة — لا روابط مكسورة (تفرضه البوابة).

---

## 6. الإنفاذ الآلي — بوابة الجودة

كل المعايير القابلة للفحص تُفرض عبر:

```bash
python3 scripts/quality_gate.py
```

تفحص البوابة: صدق الأرقام، صحّة `data.json` مقابل المخطّط، اكتمال العنوان/الوصف/canonical/JSON-LD،
تزامن `sitemap.xml`، سلامة الروابط الداخلية، وغياب مصادر XSS.

**تعمل تلقائياً** على كل `push` و `pull request` عبر
[`.github/workflows/quality-gate.yml`](./.github/workflows/quality-gate.yml).
**فشل البوابة يمنع الدمج.**

---

## 7. ضبط التغيير

1. طوّر على فرع، لا على `main` مباشرة.
2. شغّل البوابة محلياً حتى تنجح.
3. افتح PR مستوفياً قائمة [قالب الـ PR](./.github/pull_request_template.md).
4. لا دمج إلا ببوابة **خضراء** ومراجعة.

> الحوكمة ليست عائقاً — بل هي ما يجعل هذا الأصل جديراً بالثقة، ومن ثمّ جديراً بالقيمة.
